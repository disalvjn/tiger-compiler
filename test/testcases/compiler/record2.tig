let type intList = {head : int, tail : intList}
    var list := intList {head = 4, tail = nil}
in for i:= 0 to 3 do (list := intList {head = 3-i, tail = list});
   while list <> nil do (printInt(list.head); list := list.tail)
end
